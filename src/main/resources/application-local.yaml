spring:
    application:
        name: board
    datasource:
        url: jdbc:mysql://localhost:3307/board?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
        username: spring-user
        password: 1234
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                format_sql: true


#        AWS 는 ServerTimeZone=Seula 지원안하는 듯 .... ?!?! ===> 확인이 필요 !!
#logging:
#    level:
#      org.hibernate: DEBUG
#      org.springframework: DEBUG
## TODO - 테스트 후 해당 설정 프로퍼티 지우기 ... !!!
server:
    tomcat:
        connection-timeout: 10
