spring:
    application:
        name: board
    datasource:
        # url: jdbc:mysql://localhost:3307/board?useSSL=false&serverTimezone=Asis/Seoul
        url:
        username:
        password:
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
          hibernate:
              dialect: org.hibernate.dialect.MySQLDialect
              format_sql: true
#logging:
#    level:
#      org.hibernate: DEBUG
#      org.springframework: DEBUG
